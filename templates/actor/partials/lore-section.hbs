{{#if groups.length}}
<div class="details-section">
    {{#each groups}}
    <div class="stonetop-choice-entry" data-slug="{{slug}}">
        {{#each list}}
        {{> "stonetop.choice-row" inputClass="stonetop-option-check"}}
        {{/each}}
    </div>
    {{/each}}
</div>
{{/if}}
