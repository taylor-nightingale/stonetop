{{!-- Hover edit/lock pencil for a steading section. Thin wrapper over the shared
      section-edit-toggle that supplies the steading handler's class hook; the
      shared partial's `recentlyEdited` branch drives the fade-out check here. --}}
{{> "stonetop.section-edit-toggle" toggleClass="steading-section-edit-toggle" section=section editing=editing}}
